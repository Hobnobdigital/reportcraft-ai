// ThemeProvider is no longer used — light-only design.
// Kept as empty export for any residual imports.
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
